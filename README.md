# Suggest tags for stackoverflow questions

#includes Dockerfile to build a docker image.

#after building the image launch with docker run -d -p 5000:5000 <docker_image> 
#and go to localhost:5000


 


